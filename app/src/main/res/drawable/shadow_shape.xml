<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners
        android:bottomLeftRadius="@dimen/ten_dp"
        android:bottomRightRadius="@dimen/zero_dp"
        android:radius="@dimen/Fifteen_dp"
        android:topLeftRadius="@dimen/ten_dp"
        android:topRightRadius="@dimen/zero_dp" />
    <gradient

        android:centerColor="@color/scheduled_text"
        android:endColor="@color/scheduled_text"
        android:startColor="@color/scheduled_text"
        android:type="linear"
        android:angle="90"/>
    <padding
        android:bottom="@dimen/zero_dp"
        android:left="@dimen/zero_dp"
        android:right="@dimen/zero_dp"
        android:top="@dimen/zero_dp" />
</shape>
