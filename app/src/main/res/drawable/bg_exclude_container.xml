<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Soft light background -->
    <solid android:color="@color/color_blue_icon_bg"/>

    <!-- Subtle border -->
<!--    <stroke-->
<!--        android:width="1dp"-->
<!--        android:color="@color/blue_dark"/>-->

    <!-- Rounded corners -->
    <corners android:radius="@dimen/six_dp"/>

    <!-- Inner padding effect -->
    <padding
        android:left="@dimen/ten_dp"
        android:top="@dimen/ten_dp"
        android:right="@dimen/ten_dp"
        android:bottom="@dimen/ten_dp"/>

</shape>
